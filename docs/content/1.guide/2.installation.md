---
title: Installation
description: How to install and configure the Nuxt AEO module
navigation: false
---

This guide explains how to install the Nuxt AEO module in your Nuxt project.

## Requirements

- `Nuxt 3.x` or higher
- `Node.js 18.x` or higher

## Installation

::code-group
```bash [pnpm]
pnpm add nuxt-aeo
```

```bash [yarn]
yarn add nuxt-aeo
```

```bash [npm]
npm install nuxt-aeo
```

```bash [bun]
bun add nuxt-aeo
```
::

2. Add the module to `nuxt.config.ts`:

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  modules: ['nuxt-aeo'],
})
```

---

## Frequently Asked Questions

This page includes FAQPage Schema to help AI models and search engines understand common questions about installing the Nuxt AEO module.

```vue [pages/example.vue]
<script setup lang="ts">
useSchemaPage({
  mainEntity: [
    {
      name: '설치 요구사항은?',
      acceptedAnswer: {
        text: 'Nuxt AEO 모듈을 설치하려면 Nuxt 3.x 이상과 Node.js 18.x 이상이 필요합니다.',
      },
    },
    {
      name: '어떤 패키지 매니저를 사용해야 하나요?',
      acceptedAnswer: {
        text: 'pnpm, yarn, npm, bun 등 모든 주요 패키지 매니저를 지원합니다. 프로젝트에서 사용하는 패키지 매니저를 그대로 사용하면 됩니다.',
      },
    },
    {
      name: '설치 후 바로 사용할 수 있나요?',
      acceptedAnswer: {
        text: '네, 설치 후 nuxt.config.ts에 모듈을 추가하면 바로 사용할 수 있습니다. 추가 설정 없이도 기본 Project Schema가 자동으로 주입됩니다.',
      },
    },
  ],
})
</script>
```

### 질문 목록

- **설치 요구사항은?**: Nuxt 3.x 이상, Node.js 18.x 이상 필요
- **어떤 패키지 매니저를 사용해야 하나요?**: pnpm, yarn, npm, bun 등 모든 주요 패키지 매니저 지원
- **설치 후 바로 사용할 수 있나요?**: 네, 설치 후 바로 사용 가능하며 기본 Project Schema가 자동 주입됩니다

## Next Steps

- [Module Options](/guide/module-options) - Configure global schemas
- [Usage](/guide/usage) - Using composable functions

