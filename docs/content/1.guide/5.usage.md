---
title: Usage
description: How to use the composable functions of the Nuxt AEO module
---

The Nuxt AEO module provides two main composable functions:

- `useSchemaPage()`: A helper function for easily adding FAQPage Schema
- `useSchema()`: A universal function for adding any Schema type

---

## useSchemaPage

A dedicated composable for adding question-answer structures to FAQ pages.

### Basic Usage

```vue [pages/example.vue]
<script setup lang="ts">
useSchemaPage({
  mainEntity: [
    {
      name: 'What is the Nuxt AEO module?',
      acceptedAnswer: {
        text: 'Nuxt AEO is a Nuxt module that supports AI Engine Optimization (AEO) through Schema.org JSON-LD.',
      },
    },
    {
      name: 'What Schema types are supported?',
      acceptedAnswer: {
        text: 'Currently supports Schema types such as Person, FAQPage, ItemList, Article, TechArticle, etc.',
      },
    },
  ],
})
</script>
```

### `mainEntity` (Required)

An array of FAQ questions and answers.

**Type**: `Array<{ name: string, acceptedAnswer: { text: string } }>`

### `renderHtml` (Optional)

Whether to automatically generate semantic HTML.

**Type**: `boolean`:br**Default**: `true`

### `visuallyHidden` (Optional)

Whether to visually hide the generated HTML.

**Type**: `boolean`:br**Default**: `true`

### Example: FAQ Page

```vue [pages/example.vue]
<script setup lang="ts">
const faqs = [
  {
    question: 'What is the Nuxt AEO module?',
    answer: 'Nuxt AEO is a Nuxt module that supports AI Engine Optimization (AEO) through Schema.org JSON-LD.',
  },
  {
    question: 'What Schema types are supported?',
    answer: 'Currently supports Schema types such as Person, FAQPage, ItemList, Article, TechArticle, etc.',
  },
]

// Add FAQPage Schema
useSchemaPage({
  mainEntity: faqs.map(faq => ({
    name: faq.question,
    acceptedAnswer: {
      text: faq.answer,
    },
  })),
  renderHtml: true, // Generate semantic HTML
  visuallyHidden: true, // Visually hide
})
</script>

<template>
  <div>
    <h1>Frequently Asked Questions</h1>
    <div v-for="(faq, index) in faqs" :key="index">
      <h2>{{ faq.question }}</h2>
      <p>{{ faq.answer }}</p>
    </div>
  </div>
</template>
```

---

## useSchema

A universal composable for adding any Schema type.

### Basic Usage

Using `context` and `type` will automatically convert them to `@context` and `@type` internally:

```vue [pages/example.vue]
<script setup lang="ts">
// Organization Schema
useSchema({
  context: 'https://schema.org',
  type: 'Organization',
  name: 'Example Company',
  url: 'https://example.com',
  logo: 'https://example.com/logo.png',
})

// ItemList Schema
useSchema({
  context: 'https://schema.org',
  type: 'ItemList',
  name: 'Top 10 Programming Languages',
  description: 'The most popular programming languages in 2024',
  itemListElement: [
    {
      type: 'ListItem',
      position: 1,
      name: 'JavaScript',
      item: 'https://example.com/javascript',
    },
    {
      type: 'ListItem',
      position: 2,
      name: 'Python',
      item: 'https://example.com/python',
    },
  ],
})

// Person Schema
useSchema({
  context: 'https://schema.org',
  type: 'Person',
  name: 'John Doe',
  jobTitle: 'Software Engineer',
  url: 'https://example.com',
})
</script>
```

### `context` (Optional)

The Schema.org context URL.

**Type**: `string`:br**Default**: `'https://schema.org'`

### `type` (Required)

The Schema type (e.g., `'Person'`, `'Organization'`, `'Article'`, etc.).

**Type**: `string`

### `renderHtml` (Optional)

Whether to automatically generate semantic HTML.

**Type**: `boolean`:br**Default**: `false`

### `visuallyHidden` (Optional)

Whether to visually hide the generated HTML.

**Type**: `boolean`:br**Default**: `true`

### Person Schema Example

```vue [pages/example.vue]
<script setup lang="ts">
useSchema({
  context: 'https://schema.org',
  type: 'Person',
  name: 'John Doe',
  alternateName: 'JD',
  jobTitle: 'Software Engineer',
  url: 'https://example.com',
  image: 'https://example.com/profile.jpg',
  knowsAbout: ['JavaScript', 'TypeScript', 'Vue.js'],
  sameAs: [
    'https://github.com/johndoe',
    'https://twitter.com/johndoe',
  ],
})
</script>
```

### Organization Schema Example

```vue [pages/example.vue]
<script setup lang="ts">
useSchema({
  context: 'https://schema.org',
  type: 'Organization',
  name: 'My Company',
  url: 'https://www.example.com',
  logo: 'https://www.example.com/logo.png',
  description: 'A great company',
  sameAs: [
    'https://www.facebook.com/example',
    'https://www.twitter.com/example',
  ],
})
</script>
```

### ItemList Schema Example

```vue [pages/example.vue]
<script setup lang="ts">
useSchema({
  context: 'https://schema.org',
  type: 'ItemList',
  name: 'Top 10 Programming Languages',
  description: 'The most popular programming languages in 2024',
  itemListElement: [
    {
      type: 'ListItem',
      position: 1,
      name: 'JavaScript',
      item: 'https://example.com/javascript',
    },
    {
      type: 'ListItem',
      position: 2,
      name: 'Python',
      item: 'https://example.com/python',
    },
    {
      type: 'ListItem',
      position: 3,
      name: 'TypeScript',
      item: 'https://example.com/typescript',
    },
  ],
})
</script>
```

### Article Schema Example

```vue [pages/example.vue]
<script setup lang="ts">
useSchema({
  context: 'https://schema.org',
  type: 'Article',
  headline: 'My Article Title',
  description: 'Article description',
  author: {
    type: 'Person',
    name: 'John Doe',
  },
  datePublished: '2024-01-01',
  dateModified: '2024-01-02',
  image: 'https://example.com/article-image.jpg',
})
</script>
```

---

## Automatic Key Conversion

Using `context` and `type` in `useSchema` will automatically convert them to `@context` and `@type` internally. This also applies to nested objects:

```vue [pages/example.vue]
<script setup lang="ts">
useSchema({
  context: 'https://schema.org', // → "@context": "https://schema.org"
  type: 'Article', // → "@type": "Article"
  author: {
    type: 'Person', // → "@type": "Person" (also converted in nested objects)
    name: 'John Doe',
  },
})
</script>
```

Generated JSON-LD:

```json [example.json]
{
  "@context": "https://schema.org",
  "@type": "Article",
  "author": {
    "@type": "Person",
    "name": "John Doe"
  }
}
```

---

## Verification

Each composable function automatically adds a JSON-LD script tag to the page's `<head>` tag. You can check this in the Elements tab of Developer Tools (F12):

```html [example.html]
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Person",
  "name": "John Doe",
  ...
}
</script>
```

You can also use [Google's Rich Results Test](https://search.google.com/test/rich-results) to verify that the Schema is correctly recognized.

---

## Frequently Asked Questions

This page includes FAQPage Schema to help AI models and search engines understand common questions about using the Nuxt AEO module composables.

```vue [pages/example.vue]
<script setup lang="ts">
useSchemaPage({
  mainEntity: [
    {
      name: 'useSchemaPage()와 useSchema()의 차이는?',
      acceptedAnswer: {
        text: 'useSchemaPage()는 FAQPage Schema를 쉽게 추가하기 위한 전용 함수이며, renderHtml 기본값이 true입니다. useSchema()는 모든 Schema 타입을 추가할 수 있는 범용 함수이며, renderHtml 기본값이 false입니다.',
      },
    },
    {
      name: '어떤 경우에 useSchemaPage()를 사용하나요?',
      acceptedAnswer: {
        text: 'FAQ 페이지나 질문-답변 구조가 있는 페이지에서 useSchemaPage()를 사용하는 것이 좋습니다. FAQPage Schema를 간단하게 추가할 수 있으며, 시맨틱 HTML도 자동으로 생성됩니다.',
      },
    },
    {
      name: 'context와 type은 자동 변환되나요?',
      acceptedAnswer: {
        text: '네, useSchema()에서 context와 type을 사용하면 자동으로 @context와 @type으로 변환됩니다. 중첩된 객체 내부에서도 type이 자동으로 @type으로 변환됩니다.',
      },
    },
    {
      name: '여러 개의 Schema를 한 페이지에 추가할 수 있나요?',
      acceptedAnswer: {
        text: '네, useSchema()와 useSchemaPage()를 여러 번 호출하여 여러 개의 Schema를 추가할 수 있습니다. 각 호출은 독립적으로 JSON-LD를 생성하여 페이지에 추가합니다.',
      },
    },
    {
      name: '전역 스키마와 페이지별 스키마를 함께 사용할 수 있나요?',
      acceptedAnswer: {
        text: '네, 전역 스키마는 모든 페이지에 자동으로 주입되고, 페이지별로 useSchema()나 useSchemaPage()를 사용하여 추가 스키마를 추가할 수 있습니다. 두 가지가 함께 적용됩니다.',
      },
    },
  ],
})
</script>
```

### 질문 목록

- **useSchemaPage()와 useSchema()의 차이는?**: useSchemaPage()는 FAQPage 전용 함수이고 renderHtml 기본값이 true이며, useSchema()는 범용 함수로 renderHtml 기본값이 false입니다
- **어떤 경우에 useSchemaPage()를 사용하나요?**: FAQ 페이지나 질문-답변 구조가 있는 페이지에서 사용합니다
- **context와 type은 자동 변환되나요?**: 네, 자동으로 @context와 @type으로 변환되며 중첩 객체에서도 적용됩니다
- **여러 개의 Schema를 한 페이지에 추가할 수 있나요?**: 네, 여러 번 호출하여 여러 Schema를 추가할 수 있습니다
- **전역 스키마와 페이지별 스키마를 함께 사용할 수 있나요?**: 네, 두 가지가 함께 적용됩니다

## Next Steps

- [Semantic HTML](/guide/semantic-html) - Detailed explanation of semantic HTML auto-generation feature
- [Module Options](/guide/module-options) - Configuring global schemas

